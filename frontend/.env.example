# Base URL of the backend API, reachable from BOTH the browser and the
# Next.js server process (used for SSR data fetching in app/page.tsx).
# In docker-compose, expose the API on a host port and point this at it
# rather than the internal service name, since the browser can't resolve it.
NEXT_PUBLIC_API_URL=http://localhost:3000
